# Phase-C Inventory Master — Validated Knowledge (KNOWLEDGE.md)

**Platform v1.0 — FROZEN at `e2d69602` (operator ruling 2026-07-03)**

Append-only. Facts here are PROVEN from repo evidence — nothing guessed (Constitution
§18/§19). Populated at platform creation from the Phase-0-grade audits; extended by the
Phase 0 run and at each wave boundary.

Primary sources:
- `reports/inspection/2026-07-03T-integration-architecture-audit.md` (audit `b9f5664c` + amendment `b48b9f1c`)
- `reports/inspection/2026-07-02T-wfirma-wireframe-inspection.md`
- `reports/inspection/2026-07-03T-wfirma-section-e-operator-checklist.md`
- PROJECT_STATE.md `# DECISIONS` (C-1 rulings, MM business answer, lifecycle rule)

---

## Authority map (frozen — audit §VERDICT)

| Authority | Owns | Canonical store |
|---|---|---|
| **Product Mirror/Master** | product ↔ wFirma goods identity + design-number linkage | wfirma_product_mirror (consolidating 2 legacy mirrors) + product_authority_resolver |
| **Customer Master** | customer identity, VAT, commercial/freight/insurance/KYC defaults | customer_master.sqlite (business layer) + wfirma_customer_mirror (consolidating 2 caches) |
| **Inventory V2** | piece lifecycle, locations, stock promotion notes, sample/returns state | inventory_state_engine + warehouse.db (single-writer) |
| **wFirma** | accounting documents (PZ/WZ/MM/invoice), stock in wFirma warehouses | wfirma_client + document endpoints |

Permanent rule (PROJECT_STATE DECISIONS): no inventory feature starts until it names
which of these four it extends; if none — STOP and ask.

## Canonical mirror set (target: 3 tables — audit §Q6)

- `wfirma_product_mirror` — product_code PK + wfirma_product_id + design_number +
  sync fields (created in C-1a).
- `wfirma_customer_mirror` — contractor_id PK (NOT client_name) + bill_to_name (label) +
  sync fields (C-2a target; collapses `wfirma_customers` (wfirma.db) +
  `wfirma_customer_mapping` (reservation.db)).
- `wfirma_warehouse_mirror` — NET-NEW only if warehouse ids must resolve from wFirma
  (OI-14).

customer_master stays as the business-enrichment layer ABOVE the mirror (not collapsed).
Mirror field discipline (MASTER CONSUMPTION RULE): mirrors hold ONLY
wfirma_id/product_code/sync_version/last_sync/hash/deleted_flag (+design_number for
product, the one allowed extra) — never business logic.

## Module census verdicts (audit §Q0)

- Product Master: FRAGMENTED pre-C-1 (design linkage clean via one resolver; goods
  identity was split across 2 mirrors) — C-1a..C-1w1 consolidating; C-1w2/C-1e/C-1f/C-1d remain.
- Customer Master: REAL but FRAGMENTED (2 name-keyed caches; invoice XML sources
  wfirma_customer_id from a cache, not the Master) — C-2 target.
- Inventory: REAL — single-writer transition() discipline; strongest authority.
- Packing: REAL. PZ: REAL (live-proven, gated). Invoice: REAL (webhook pipeline works).
- WZ: ABSENT (registered module string, no create fn; SALES_TRANSIT never fired).
- Sample: REAL but WRITE-ONLY (no read/list endpoint — the Phase-C gap).
- Returns: REAL but WRITE-ONLY (+ returns_events migration NOT applied in prod).
- Consignment: ABSENT (no state/table/route; ConsignmentTab stub loaded, mounted nowhere).

## Authority violations remaining (audit §V-list; V1/V6 fixed in C-1b)

- V2 two product mirrors → consolidation completing through C-1 slices
- V3 two customer caches → C-2a
- V4 routes_proforma customer fallback (:1500,1529,7767,7800,1877,3637,8067) → C-2b
- V5 routes_ledgers (:155,288) direct fetch_contractor_by_id → C-2b
- V7 routes_suppliers (:396) + customer_master's own direct mapping (design-debt) → C-2b

## Sync matrix (audit §Q7)

| Object | API | Webhook handler | Scheduled sync |
|---|---|---|---|
| Product | YES (goods add/find/edit) | NO (no goods handler — would dead-letter) | NO |
| Customer | YES (contractors) | INDIRECT (invoice events, Phase 3) | YES (Phase 3B 6h poll) |
| Invoice | YES | YES (Faktury.* → snapshot) | via webhook |
| PZ | YES (live-proven) | NO | on-demand |
| WZ | PARTIAL (registered, no create fn) | NO | NO |
| MM | NOT MODELED | NO | NO |
| Payment | YES (read-only) | NO | YES (Phase 4A 1h) |

Webhook auth = HMAC vs WFIRMA_WEBHOOK_KEY (503 if unset — OI-7). Pipeline is
invoice-specific.

## wFirma client capability facts (wireframe inspection §B)

- Live: PZ create/fetch/find (gated) · reservation · proforma/invoice suite ·
  contractors suite · goods get/create/edit · list_warehouses · fetch_series.
- get_stock = NotImplementedError stub (:1161).
- Registered-no-create-fn: PW, RW, **WZ**, ZD.
- **MM absent from every layer** (client, python-wfirma types, all four wFirma docs).
- Stale-docs warning: 2023 forum claims API warehouse-doc creation impossible +
  "WZ auto-generated by invoice" — production DISPROVED this for PZ; WZ/MM remain
  unverified → OI-3 probe decides C-2's WZ shape.

## Operator lifecycle rules (verbatim, PROJECT_STATE DECISIONS)

- Inventory temp states are document-event-driven, synchronized with wFirma: "Temp
  Purchase closes AUTOMATICALLY on PZ creation… WZ creation moves goods out → Temp Sale
  (in transit to customer)… DHL delivery confirmation closes Temp Sale. Manual Move Stock
  page = exception/correction path only; the document is the primary trigger."
- Consignment (binding): issue = wFirma MM internal transfer (Main → Consignment
  warehouse), NOT a sale WZ; the invoice consumes CONSIGNMENT-warehouse stock only —
  prevents double stock-out. (MM business model ANSWERED 2026-07-03; API vehicle = OI-1.)

## Backend gaps our side (independent of wFirma — wireframe inspection §B)

- Invoice→SALES_TRANSIT transition never fired (routes_proforma.py:3311-4001 has no
  transition() call; trigger 'invoice_issued' defined but unreachable) → C-3d.
- Consignment model absent → C-4a.
- returns_events migration draft_20260512_175238 not applied → C-3a (deploy-gated).
- Piece↔invoice-line linkage preview-time-only (no persisted reservation binding).
- client_po: FIXED end-to-end 2026-07-02 (B3, `0602ddd3`) — wireframe gap #3 closed.

## Buildable now without operator input (audit §queue)

Wave 1 entirely (app-side); C-3b/C-3c/C-3d/C-3e (app-side); C-3a (deploy-gated only).
Gated: C-4a (OI-17), C-4b (OI-1/2/14 — fallback path exists), C-6a (OI-3),
all Wave 4 (OI-1/3/4/7/9/10/11).

---

## Phase 0 findings (run 2026-07-03, platform commit 575bb3f3)

- **Regression baseline GREEN:** root golden suite 160/160 (`test_pz_regression.py`,
  PYTHONUTF8=1; `make` binary absent from session PATH — underlying TEST target run
  directly, equivalent).
- **Consumption pin GREEN 8/8:** `test_master_consumption_rule.py` — mirror schema
  exactly six columns; no new product-direct violations; baseline = 3 files
  (routes_wfirma_capabilities.py, routes_proforma.py, routes_wfirma.py) — matches the
  declared C-1c residual exactly (targets: C-1w2, C-1f, C-1e).
- **Wireframe hash (full, for W2-A5/W3-A2 boundary checks):**
  `f7dd5e3889660fdc1ef76da0f1424a11cad512e7202650db10c031a57799699a`
  (docs/design/estrella-dashboard-wireframe.html).
- **sample_out_events confirmed on disk:** written by inventory_sample_writer.py,
  schema in warehouse_db.py, read by inventory_piece_view.py → W2-A1 VALID.
- **returns_events migration draft confirmed:**
  `service/app/db/migrations/draft_20260512_175238_returns_events.py.draft` — apply
  remains CP4 deploy-gated (C-3a) → W2-A2 VALID as draft (apply-safety verified at
  slice time).
- **contractor_id already load-bearing in Customer Master:**
  `bill_to_contractor_id` is a REQUIRED field (customer_master_db.py:46,175) and
  Phase 3/3B sync fetches contractors by id in production without reported breakage →
  W1-A3 VALID (production reliance), OI-13 stays OPEN for formal wFirma confirmation of
  stability in webhook payloads/invoice blocks.
- **Git reconcile:** HEAD `575bb3f3` (platform launch commit); RUNTIME completed-slice
  ledger matches git log — no unrecorded slices found.
- **OI evidence pass:** no OI answerable from repo evidence beyond OI-1's business half
  (already ANSWERED) — OIs 1..18 states unchanged; full ledger in OPEN_ITEMS.md.

## Wave-boundary updates (append at each boundary)

### Wave-2, C-3g (2026-07-03) — mirror-only reads + passthrough retirement
- Product pin at TRUE 0: KNOWN_PRODUCT_VIOLATION_FILES = {} (empty, pinned to
  stay empty); routes_proforma / routes_wfirma / routes_wfirma_capabilities all
  measure zero real-access hits.
- rdb.get_cached_product/_batch/list_cached_products DELETED; capabilities is a
  declared product-sync surface (whitelist, customer-pin precedent); sync-state
  queries: product_authority_resolver.get_registered_goods_state(_batch).
- Service-charge emission metadata: pildb service_product_registry
  (proforma_links.db); identity stays mirror-only. Backfill tool:
  service/tools/backfill_service_product_registry.py.
- C-1f had shipped a NameError on every MAPPED service-charge emission
  (_build_service_charge_lines `prod` dangling ref) — fixed + pinned in C-3g.
- wfirma_id collision in verify-tree data: goods id 99 claimed by
  EJL/26-27/254-1 (mirror owner) AND EJL/26-27/257-2 (unresolved) — deploy note
  requires operator resolution of prod collisions (c3g-deploy-note.md).
- reservation_db.register_product_identity STILL dual-writes mirror+cache — 
  intentionally: the cache remains the sync layer's own store (capabilities
  listing, auto-register lifecycle); only BUSINESS reads of it were retired.
  Full cache collapse into the 3-table canonical set remains future scope
  (audit §Q6 target).

### Wave-1 boundary (2026-07-03) — C-1d census
- Pin baseline 5 → 1 (routes_proforma single transitional dual-write; not a read).
- Customer full-app pin at zero. Inventory grep clean.
- Mirror completeness: COMPLETE (proof 37aaaf27; 3 gaps closed).
- Out-of-pin-scope census (disposition at ratification): global_pz_push,
  wfirma_reservation, wfirma_reservation_create (services);
  build_pz_batch, send_wfirma_good_live_test, send_wfirma_proforma_live_test (tools).
- W2-A3 (single-writer discipline intact after Wave 1): re-verified — no Wave-1
  slice touched inventory_state_engine or its writers → VALID.
- Full audit: reports/implement/2026-07-03T190000Z/c1d-verification-audit.md
