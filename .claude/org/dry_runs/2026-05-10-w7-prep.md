# W-7 Stabilization Prep — 2026-05-10

**Mode:** PRE-IMPLEMENTATION
**Scope:** enumerate dashboard test failures and produce a repair
map without touching code/tests
**Baseline:** `758ae78` (post-A3)

## Failure enumeration

`pytest tests/test_dashboard*.py --tb=no -q` → **865 passed, 10
failed**. Network-hanging tests: none observed (all failures
deterministic, suite completes in <7s). Suite-isolation leaks:
none observed (failure set is the same on repeat runs).

| # | Failure | Test file | Group |
|---|---|---|---|
| F1 | `test_no_auto_create_product_endpoint_referenced` — forbidden marker `'goods/add'` present in dashboard.html | test_dashboard_wfirma_search.py | **forbidden marker** |
| F2 | `test_route_audit_zero_stale` — stale (missing) route reference at dashboard.html line 12791 | test_dashboard_repair.py | **stale route** |
| F3 | `test_closure_eval_is_read_only` — `/check` substring missing inside closure-eval-card | test_dashboard_execution_guards.py | **execution-guard mismatch** |
| F4 | `test_closure_confirm_body_includes_empty_payload` — closure_confirm POST body missing literal `payload: {}` | test_dashboard_execution_guards.py | **execution-guard mismatch** |
| F5 | `test_confirm_button_sends_approved_by_in_payload` — closure-confirm-btn payload missing `approved_by` | test_dashboard_closure_eval_card.py | **execution-guard mismatch** |
| F6 | `test_confirm_refreshes_after_success` — closure confirm handler missing `loadBatchReadiness()` on success | test_dashboard_closure_eval_card.py | **execution-guard mismatch** |
| F7 | `test_dhl_docs_upload_posts_to_upload_endpoint` — DHL docs card missing `/api/v1/dhl-documents/` endpoint reference | test_dashboard_dhl_documents_received_card.py | **card wiring** |
| F8 | `test_agency_docs_card_in_dhl_tab` — `agency-docs-received-card` not inside DHL/Customs tab block | test_dashboard_agency_docs_card.py | **card wiring** |
| F9 | `test_refresh_dhl_readiness_after_upload` — agency-docs upload handler missing `loadDhlReadiness()` call | test_dashboard_agency_docs_card.py | **card wiring** |
| F10 | `test_refresh_batch_readiness_after_upload` — agency-docs upload handler missing `loadBatchReadiness()` call | test_dashboard_agency_docs_card.py | **card wiring** |

## Group classification (per /context taxonomy)

| Group | Count | Items |
|---|---|---|
| Stale route references | 1 | F2 |
| Forbidden markers | 1 | F1 |
| Execution-guard mismatches | 4 | F3, F4, F5, F6 |
| Network-hanging tests | 0 | — |
| Suite-isolation leaks | 0 | — |
| Card wiring (upload + tab placement + readiness-refresh) | 4 | F7, F8, F9, F10 |

## Files affected

| File | Affected by | Fix class |
|---|---|---|
| `ui/dashboard.html` | F1, F2, F3, F4, F5, F6, F7, F8, F9, F10 (all 10) | **HTML-only** |

Every failing test asserts against the contents of `ui/dashboard.html`.
Zero failures require backend-route or schema changes. The repair
campaign is therefore single-file at the production-code level.

## Smallest-safe repair order

```
B1.a  Forbidden-marker + stale-route cleanup       (smallest, most isolated)
        F1  remove the literal 'goods/add' substring from dashboard.html
        F2  remove or update the stale route reference at line 12791
      Expected diff: ≤ 5 lines
      Risk: trivial — both are pure deletions
      Tests gated: 2

B1.b  Closure execution-guard contract             (small, narrow)
        F3  add `/check` reference inside closure-eval-card
        F4  add literal `payload: {}` to closure_confirm POST body
        F5  include `approved_by` in closure-confirm-btn payload
        F6  call `loadBatchReadiness()` after closure-confirm success
      Expected diff: ~10-15 lines, all inside the closure-eval-card block
      Risk: low — execution-guard contract pinned by 4 tests
      Tests gated: 4

B1.c  Card-wiring repair                           (largest)
        F8  move agency-docs-received-card inside DHL/Customs tab block
        F9  call loadDhlReadiness() after agency-docs upload
        F10 call loadBatchReadiness() after agency-docs upload
        F7  reference /api/v1/dhl-documents/ endpoint in DHL docs card
      Expected diff: ~20-30 lines across two cards
      Risk: low-moderate — card placement is structural; readiness refresh
            calls must hit the right scope
      Tests gated: 4
```

Three commits total. None touch `service/**`, `tests/**`, or any
backend route. All ten failures resolve to single-file edits in
`ui/dashboard.html`.

## Lane-serialization confirmation

W-7 will be the **first code lane to engage the serialization
gate** (per ADR-011 / lane-serialization clause).

- **W-7 scope:** `ui/dashboard.html` only.
- **Concurrent lanes prohibited:** any session attempting to
  edit `service/**` or `ui/**` while W-7 is in flight must be
  rejected at session-header time.
- **Concurrent lanes permitted:** Lane A (governance / docs) and
  Lane C (DL-G1 RELEASE inspection — produces no source changes)
  remain parallel-eligible.
- **Conflict surface:** Lane C's checklist walk reads
  `ui/dashboard.html`; reads do not violate serialization.

## DL-G1 parallelization legality

DL-G1 (RELEASE-mode W-1 audit) can open **after B1.a commits** —
i.e., as soon as the dashboard test suite is *visibly trending
green* and at least the trivial-cleanup commit has demonstrated
W-7 lane discipline. Strictly, DL-G1 could open *now* (it is
inspection-only, no source changes), but the operator's
recommended sequence (governance lane confirms no surprises
first) is the safer order.

## Exit gate

**clean.** B1-prep produces this repair map and stops. No code
edited. No tests edited. The B1 implementation campaign opens in
a fresh session.

## Open questions for B1 implementation

1. Is line 12791's stale route an obsolete legacy reference, or
   is it a typo of a current route? B1.a should resolve by
   reading the test's current pinning logic (it enumerates routes
   the dashboard calls vs. routes registered on the FastAPI app)
   and checking whether the line should be removed or corrected.

2. Does `loadBatchReadiness()` already exist as a helper in
   dashboard.html? If yes, the fix is one new call site. If no,
   the helper must also be added — slightly larger diff.

3. The closure-confirm payload contract (`approved_by` + `payload: {}`)
   suggests the backend route `/api/v1/execute/closure_confirm`
   expects both fields. Verifying the route shape is a read-only
   precondition for B1.b but does not require backend edits.
