fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
httpx>=0.27.0
python-multipart>=0.0.9
bcrypt>=4.0.0
PyJWT>=2.8.0

# Engine dependencies (already required by pz_import_processor.py)
reportlab>=4.0
openpyxl>=3.1
# xlrd 2.0+ removed legacy .xls support; pin to the 1.2.x line so the
# packing parser can open .xls vendor files (purchase packing lists).
xlrd>=1.2.0,<2.0.0
pymupdf>=1.24          # fitz — PDF parsing
pdfplumber>=0.10       # Polish-description PDF text validator (routes_dhl_clearance.py)
pypdf>=6.0             # PDF text extraction for polish-description engine tests
requests>=2.31         # NBP API
tzdata>=2024.1         # IANA tz database for ZoneInfo on Windows (warsaw_today())
anthropic>=0.50.0      # Anthropic Python SDK — required by ai_gateway._anthropic_call()

# Excel ingestion stack — shipment uploads, packing lists, customer master, invoice parsing
pandas>=2.0.0          # DataFrame engine for all Excel/CSV ingestion
pyxlsb>=1.0.10         # Binary .xlsb Excel format support
rapidfuzz>=3.0.0       # Fuzzy string matching for AI column mapping
xlsxwriter>=3.0.0      # XLSX generation (reports, exports)
python-dateutil>=2.9.0 # Robust date parsing across Excel date formats
chardet>=5.0.0         # Byte-stream charset detection for uploaded files
