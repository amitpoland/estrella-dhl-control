# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.log
# Carve-out: reports/authority-census/**/*.log are audit evidence
# (D-4 drift, D-5 extras) — tracked, not transient.
!reports/authority-census/**/*.log
# Virtual env
venv/
.venv/
service/.venv/

# Claude Code / Ruflo MCP sandbox artifacts
.claude/worktrees/
.claude/agents/prompt-engineer.md
# PROJECT_STATE.md is the operational ledger; it accumulates third-party PII and
# is kept local-only (Option A forward-exposure fix). Readers use the on-disk copy.
.claude/memory/PROJECT_STATE.md
.claude/memory/PROJECT_STATE.LOCAL_BACKUP.md
.claude/.regression-state
.claude/.allow-red-stop
.env
# OS
.DS_Store
# Storage (VERY IMPORTANT)
service/app/storage/
service/storage/
# AI Bridge runtime files
ai_bridge/tasks/
ai_bridge/processed/
ai_bridge/errors/
# Quarantine folders
service/app/storage_quarantine_*/
tmp/claude_test_quarantine_*/

# Safety catch for any future variants
service/app/*quarantine*/
tmp/*quarantine*/

# Claude Code runtime artifacts (future-safe)
service/.claude/locks/
service/.claude/snapshots/

# Secrets / configs
*.env
.env.*

# Local runtime / generated artefacts
.claude/settings.local.json
service/MagicMock/
MagicMock/
service/packing.db
outputs/
storage/
invoice_learning_store.json
parser_fix_proposals.json
PZ_READY_*.json

# C15A: deploy validation scripts generated per-campaign (SHA-suffixed, not for tracking)
validate_deploy_*.sh

# Local runtime databases
service/proforma_links.db

# Operational audit reports may contain real customer PII — keep local, not published (PII phase-2, cat 3)
tasks/reports/

# Operational campaign-state registry (mutable live state — never tracked; operator ruling 2026-07-17)
.claude/state/
