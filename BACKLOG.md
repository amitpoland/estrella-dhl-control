# BACKLOG.md

Side-discoveries captured during task execution that are out of scope for the current task.
Each entry must receive a SCHEDULED / ISSUE / REJECTED disposition (GATE 4) before the
next task closes.

---

## Entries

| # | Discovery | Found during | Disposition | Notes |
|---|---|---|---|---|
| B-001 | PR #661 (`ci/auto-merge-approved`) is non-draft and unreviewed — appears stale since 2026-06-20 | /feature DISCOVERY | SCHEDULED — review before next merge sprint | Verify it doesn't conflict with governance gates before approving |
| B-002 | `store_sales_document` uses `INSERT OR REPLACE` with a fresh-UUID PK and no `UNIQUE(batch_id, document_id)` — repeat calls create duplicate sales_documents rows (PRE-EXISTING; not introduced by PR-2). | PR-2 backend-safety review (F1) | CLOSED — ALREADY_FIXED (#1209 / `eaaa005f`). `store_sales_document` delegates to `ensure_sales_document_id` (id==document_id); no random UUID mint. | Pin: `service/tests/test_b002_sales_document_identity.py`. Do not reopen. |
| B-003 | `replace_sales_packing_lines` returns the pre-DELETE count as `deleted` rather than actual rows removed (`SELECT changes()`); a failed DELETE reports a non-zero count (PRE-EXISTING). | PR-2 backend-safety review (F4) | CLOSED — ALREADY_FIXED (#1211 / `497f812c`). `deleted` uses `SELECT changes()`. | Pin: `service/tests/test_b003_replace_deleted_count.py`. Do not reopen. |
| B-004 | `auto_create_draft_from_sales_packing` writes the `created_from_sales_packing` event on a second connection outside the draft INSERT txn; a crash between leaves a draft with no creation event (PRE-EXISTING). | PR-2 backend-safety review (F5) | CLOSED — ALREADY_FIXED (#1213 / `80b7ae09`). Draft birth + creation event are atomic. | Pin: `service/tests/test_b004_draft_birth_atomic.py`. Do not reopen. |
| B-005 | No end-to-end test asserts `get_reservation_preview` output carries `client_contractor_id` / `contractor_resolved`. Needs a full batch fixture (audit.json + packing.db + stock). | PR-2 test-coverage review (#5) | CLOSED — ALREADY_FIXED / test pin only (#1214 / `1d53dd66`). | Pin: `service/tests/test_b005_reservation_preview_contractor.py`. Do not reopen. |
| B-006 | Broad single-process `pytest -k` run (4800+ tests) trips pre-existing failures + STORAGE-LEAK guard cascade in a fresh worktree (e.g. `test_reservation_queue` 404 reproduces on clean origin/main 5242417). | PR-2 VERIFY | CLOSED — ALREADY_FIXED / test pin only (B-017 lifespan auth path + conftest session sandbox). Fresh-process dual-run import silent; no App delta / no deploy. | Authoritative signal remains isolated/smoke suites; pin: `service/tests/test_b006_storage_import_silence.py`. Do not reopen. |
| B-007 | `enrich_lines_from_product_descriptions` unconditionally overwrites `name_pl` from the product-descriptions authority even when a line carries an operator-confirmed non-blank `name_pl` (the `_birth_resolve_name_pl` guard is not replicated). PRE-EXISTING; PR-2 does not add new `enrich_draft_lines` call sites. | PR-2 final challenge (F1) | CLOSED — ALREADY_FIXED / test pin only. Enrichment already mirrors `_birth_resolve_name_pl` provenance (`operator_kept` when `name_pl_source=operator`); no App delta / no deploy. | Pin: `service/tests/test_b007_name_pl_enrichment_guard.py`. Do not reopen. |
| B-008 | `contractor_conflict` draft-birth blocks are advisory and persist (open) while the ambiguity stands; a `list_draft_birth_blocks` code filter / `include_advisory=false` would let operators separate advisory conflicts from true non-creation blocks. | PR-2 final challenge (F4) | CLOSED — REAL_DEFECT read-side filter. `list_draft_birth_blocks`/`GET .../blocks` gain `include_advisory` (default True); creation UI uses `false`; advisory panel preserves Lesson M. Lifecycle unchanged; no schema migration. | Pin: `service/tests/test_b008_draft_birth_blocks_advisory_filter.py`. Do not reopen. |
| B-009 | The backfill log message conflates a rename-path charge collision (`canonical_already_has_charge_type`) with a canonical-wins drop (`canonical_wins_collision`) under one "DROPPED" WARNING. The per-entry `reason` field distinguishes them; the summary log does not. | PR-3 final challenge (F4) | CLOSED — REAL_DEFECT log split. `_log_backfill_dropped_charges`: rename-path → INFO; canonical-wins → WARNING; unknown → WARNING. Response `dropped_charges` unchanged. | Pin: `service/tests/test_b009_backfill_drop_log_severity.py`. Do not reopen. |
| B-010 | No isolated unit test for `set_sales_client_name` multi-client non-clobber (two distinct client_names on one sales_document, assert only the old-name lines are renamed). Covered indirectly via the forward-sync integration test. | PR-3 final consistency review | CLOSED — ALREADY_FIXED / test pin only. Code already scopes by `client_name=?` when `old_client_name` is supplied. | Pin: `service/tests/test_b010_set_sales_client_name_scope.py`. Do not reopen. |
| B-011 | `migrate_draft_to_canonical_name` is not listed in `proforma_invoice_link_db.__all__` (nor are the PR-2 `record_/resolve_/list_draft_birth_block` helpers). Imported by name so no runtime break; invisible to static export checks. | PR-3 final consistency review | CLOSED — REAL_DEFECT cosmetic public-export gap. Four symbols added to `__all__`; no runtime/API/persistence change. | Pin: `service/tests/test_b011_proforma_public_exports.py`. Do not reopen. |
| B-012 | `ProformaDraftLineRow` Save button (`btn-line-save-*`) is labelled bare "Save" — frontend standard §5.3/§7 wants the write destination ("Save to draft"). PRE-EXISTING; the proforma-authority-ui PR added a sub-element to the same cell but did not touch the button. | proforma-authority-ui frontend review | CLOSED — REAL_DEFECT label. Prod surface `shipment-detail.html` (+ `dashboard.html` mirror) → "Save to draft"; testid unchanged. | Pin: `service/tests/test_b012_save_to_draft_label.py`. Do not reopen. |
| B-013 | wFirma proforma/invoice LINE NAME posts from `design_no or product_code` (routes_proforma.py:1553/7254), NOT the canonical product description. Operator now sees the rich canonical description inline (display-only) but the wFirma line still shows design_no/code. | proforma-authority-ui plan/review | CLOSED — OPERATOR_DECISION. Status quo retained: post uses `design_no or product_code`. Canonical `description_line` sync remains opt-in via gated `POST .../refresh-line-names` (`WFIRMA_EDIT_INVOICE_ALLOWED`). No live wFirma write in this closure. | Finance/Accounting must explicitly approve before changing default posted line text. Do not reopen without that sign-off. |
| B-014 | Proforma V2 surfaces exist (`v2/proforma-detail.jsx` near-complete; `proforma-v2.html` partial) but V1 `shipment-detail.html` is the active prod proforma surface. V2 already separates customer authority from lines (tabs) but lacks granular ship-to/payment forms + birth-block surfacing. | proforma-authority-ui discovery (D) | CLOSED — FEATURE parity + cutover HOLD. Re-measure: V2 already has ship-to/payment editors; birth-block panels wired on V2 list (same contractor-projection API as V1). **Prod default remains V1** until operator cutover approval. | Cutover checkpoint: see `.claude/memory/b014-cutover-checkpoint.md`. Do not switch router without approval. |
| B-015 | MISSING_SKILL: `proforma-engine` — planned but not installed; `backend-route-and-service-builder` used as fallback for PROFORMA domain | /feature DISCOVERY (proforma readiness task) | CLOSED — STALE / GOVERNANCE. Planned skill **CANCELLED** under Skill Freeze (2026-07-04). PROFORMA routes to `ej-dashboard-fullstack-governance`. | `.claude/SKILL_ROUTING.md` updated. Do not install `proforma-engine`. |
| B-016 | `test_real_smoke_reports_are_markdown` failing on `b0-wfirma-identity-cache-review-assign.md` (missing "Verdict" field) — pre-existing noise, appeared in both Row #1 and Row #2 sessions | /feature VERIFY (DHL diagnostics task) | CLOSED — REAL_DEFECT report gap. Added `**Verdict:** PASS` to the historical smoke report (validator unchanged). | Pin: existing `test_smoke_framework.py::test_real_smoke_reports_are_markdown`. Do not reopen. |
| B-017 | `service/app/main.py:131-135` computes the auth DB path (`settings.storage_root / "users.db"`) at module import time — first `import app.main` in a fresh checkout creates `users.db` in the live storage root before any test patch applies (one-time storage-leak guard failure; fresh-checkout trap). | Phase-C Wave-2 ratification (operator amendment 4; found during storage-leak fix verification) | CLOSED — ALREADY_FIXED (PR #1207 / `198050f2`). Lifespan uses `resolve_auth_db_path`; import-time `_auth_db` gone. Fresh-process silence ×2 proven via `test_b017_*`. | Pin: `service/tests/test_b017_auth_db_import_time.py`. Do not reopen. |
| B-018 | `shipment-detail.html` regrew past the Phase-2B prune budget: 1,013,350 bytes / 17,202 lines vs the <1 MB / <14,100-line target (C6-T4 had achieved 14,039). Growth came from post-freeze V1 additions (proforma authority UI, PZ lifecycle panels), each individually approved as V1 critical fixes — but cumulatively the prune achievement is gone. Size pins converted to a growth ratchet at current size (test_phase2b_shipment_detail_pruned.py). | Phase-C Wave-2 R3 test-health slice | CLOSED — GOVERNANCE. Phase-2B prune **TARGET formally RETIRED**; growth **RATCHET retained** (Lesson F). Re-prune rejected (Lesson M — approved V1 surfaces). Ratchet numbers NOT bumped. | `test_phase2b_shipment_detail_pruned.py` docstring. Do not reopen / do not bump ratchet without new ruling. |
| B-019 | Standing prod DB backup authority completed 2026-08-14: online-backup `run_backup()`, registry=30, `GET /api/v1/admin/backup/status`, Admin Users Run Now, daily `EstrellaDBBackup` schtask, restore drill PASS. | Wave12 / B-019 | CLOSED_FEATURE_COMPLETE — App `77268073` (PR #1224); scripts installer fix PR #1225 `1dd085b9` (no App redeploy); backup_id `2026-08-13-231701` validate 30/30 PASS; schtask Ready daily 02:15 SYSTEM; rollback `772680739bfe710f2add88f312f61cedbadccf34-20260814-011613`; external writes during smoke=0 | Do not reopen unless backup/scheduler regression. Next: **B-021** (RO). |
| B-020 | **Consumer-side batch identity ambiguity (customs/carrier).** Four sites resolve a batch's contractor with `SELECT ..._contractor_id FROM shipment_documents WHERE batch_id=? AND ..._contractor_id != '' LIMIT 1` — no `ORDER BY`, no `document_type` filter: `routes_dhl_clearance.py:198`, `ai_reverification.py:159` and `:185`, `carrier/doc_package.py:341`. On a multi-party batch the selected row is non-deterministic, and the scan also reads AWB/carnet/other rows that inherited the shipment-level id at `routes_intake.py:710-711` / `1491-1495`. Independent of the intake seed defect fixed in `fix/intake-multiparty-contractor-seed`; those consumers were deliberately left untouched there. | INTAKE RESIDUAL AUTHORITY AUDIT (2026-08-12), reader-map step | CLOSED — PR #1202 / merge `0b2020a8`; `document_party_authority` NONE/SINGLE/AMBIGUOUS on production (2026-08-12). Do not reopen. | Consumer ambiguity closed; producer-side intake seed is PR #1201 / B-021 residue. |
| B-021 | **Historical contaminated `packing_contractor_resolution` rows.** Batches taken in before the intake `_sole_cid()` guard may carry a batch-level row seeded from the first non-empty contractor id at `status='confirmed', confidence=1.0` — an identity the operator never confirmed at batch level. The code fix is forward-only and intentionally performs no cleanup. | INTAKE RESIDUAL AUTHORITY AUDIT (2026-08-12) | SCHEDULED — read-only assessment first (count affected batches: rows whose batch has >1 distinct contractor on the document chain), then explicit operator authorization before any mutation | **No mutation authorized.** Production-data integrity. Consumed only via `_resolve_customer` step 0b, which is reached only when the per-document paths (0-pre, 0a) both miss. |

---

_Maintained per TASK_EXECUTION_PROTOCOL.md §Standing Rules — BACKLOG rule._
