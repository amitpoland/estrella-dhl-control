# Wave-3 Build Record — Proforma List + Detail (Page B)

**Date:** 2026-07-04  
**Branch:** deploy/latest  
**Session:** Wave-3 order #6  
**Files edited:**  
- `service/app/static/v2/proforma-list.jsx` (rewritten, ~134 → ~565 lines)  
- `service/app/static/v2/proforma-detail.jsx` (expanded, +~120 lines across 4 edit points)  
- `service/app/static/v2/pz-api.js` (added Wave-3 transport block, +~70 lines)

**Pre-edit git-status tracked-M count:** 6  
**Post-edit git-status tracked-M count:** 10 (proforma-list.jsx, proforma-detail.jsx, pz-api.js, wireframe-update.jsx)

---

## Census gaps addressed

### Proforma List gaps

| Gap ID | Description | Tag | Resolution |
|--------|-------------|-----|------------|
| PL-1   | Pipeline KPI strip — no `GET /proforma/pipeline/{batch_id}` call | BUILD | `PipelineKpiStrip` component added. Calls `PzApi.getProformaPipeline(batchId)`. Shows Draft/Editing/Approved/Posted/Error/Total tiles. `data-testid="proforma-pipeline-kpi-strip"`. Fix applied: maps `pipeline.by_state` (actual API key) not `pipeline.counts`. |
| PL-2   | Checkbox column + Match chip in table | BUILD | Checkbox column (select-all + per-row `data-testid="select-draft-{id}"`). Match chip reads `d.customer_resolved` → matched/ambiguous/unmapped badge with `data-testid="match-chip-{id}"`. |
| PL-3   | New Draft modal | BUILD | `NewProformaDraftModal` with 4 source option buttons (from_packing, from_existing, manual, from_sales). POSTs to `POST /api/v1/proforma/create/{batch_id}/{client_name}`. `data-testid="btn-new-proforma-draft"`. |
| PL-4   | Import Packing List modal | BUILD | `ImportPackingListModal` — 4-step wizard. Selects existing draft, uploads TSV, POSTs to `POST /api/v1/proforma/draft/{id}/import-sales-prices`. `data-testid="btn-import-packing-list"`. |
| PL-5   | Service-products wiring — GET/PUT `/proforma/service-products` never called | BUILD | Implemented as `ServiceProductRegistryPanel` in proforma-detail.jsx Overview tab. Calls `PzApi.getServiceProducts()` on mount; save calls `PzApi.putServiceProduct(chargeType, body)`. `data-testid="service-product-registry-panel"`. |

### Proforma Detail gaps

| Gap ID | Description | Tag | Resolution |
|--------|-------------|-----|------------|
| PD-1   | Toolbar buttons | OPERATOR-RULED | 17-button toolbar matches existing backend capabilities exactly. Not changed — OPERATOR-RULED. |
| PD-2   | Lines table — 8 columns only; wireframe shows 16 | BUILD | Expanded to 12 columns: #, PRODUCT, DESIGN NO, DESC (PL), DESC (EN), PURITY, HS CODE, ORIGIN, CUR, QTY, UNIT, NET. Horizontal scroll via `overflow: 'auto'` + `minWidth: 900`. Added `_raw: ln` preservation in lines mapping for design_no access. |
| PD-3   | Customer Matching tab | OPERATOR-RULED | ReservationTab is existing authority. Not changed — OPERATOR-RULED. |
| PD-4   | Service charges surface | BUILD | `ServiceProductRegistryPanel` added to Overview tab (wires the never-called `GET/PUT /proforma/service-products`). |
| PD-5   | Audit Trail tab | ALREADY BUILT | `history` tab in `PROFORMA_TABS` at proforma-detail.jsx line 18 (pre-existing). Confirmed rendering: "ACTIVITY HISTORY" with timestamped events. |

---

## Control Matrix — Proforma List

| Control | Wireframe Required | Implemented | Backend endpoint | Status |
|---------|--------------------|-------------|-----------------|--------|
| Pipeline KPI strip | Yes | `PipelineKpiStrip` in proforma-list.jsx | `GET /api/v1/proforma/pipeline/{batch_id}` | CLOSED (PL-1) |
| "+ New Draft" button | Yes | toolbar button + `NewProformaDraftModal` | `POST /api/v1/proforma/create/{batch_id}/{client_name}` | CLOSED (PL-3) |
| "↙ Import prices" button | Yes | toolbar button + `ImportPackingListModal` | `POST /api/v1/proforma/draft/{id}/import-sales-prices` | CLOSED (PL-4) |
| Checkbox select-all + per-row | Yes | checkbox column in table | client-side selection state | CLOSED (PL-2) |
| Match chip per row | Yes | `match-chip-{id}` badge | `d.customer_resolved` / `d.customer_ambiguous` fields | CLOSED (PL-2) |
| Export CSV | Existing | toolbar button (pre-existing) | existing export route | ALREADY BUILT |
| Search All Drafts | Existing | toolbar button (pre-existing) | `GET /api/v1/proforma/search` | ALREADY BUILT |

**Wireframe-Required Missing = 0**

---

## Control Matrix — Proforma Detail

| Control | Wireframe Required | Implemented | Backend endpoint | Status |
|---------|--------------------|-------------|-----------------|--------|
| 12-column Lines table | Yes | `ProformaLinesTab` in proforma-detail.jsx | `GET /api/v1/proforma/draft/{id}` — `editable_lines` array | CLOSED (PD-2) |
| DESIGN NO column | Yes | `ln._raw.design_no` via `_raw` preservation | same draft endpoint | CLOSED (PD-2) |
| Service Product Registry | Yes | `ServiceProductRegistryPanel` in Overview tab | `GET/PUT /api/v1/proforma/service-products/{type}` | CLOSED (PL-5/PD-4) |
| Audit Trail / History tab | Yes | pre-existing `history` tab → `HistoryTab` component | `GET /api/v1/proforma/draft/{id}/events` | ALREADY BUILT |
| Full toolbar (17 buttons) | Yes (OPERATOR-RULED) | pre-existing toolbar | existing routes_proforma.py | OPERATOR-RULED |
| Customer Matching tab | Yes (OPERATOR-RULED) | pre-existing ReservationTab | existing reservation routes | OPERATOR-RULED |

**Wireframe-Required Missing = 0**

---

## pz-api.js transport additions

All new methods are transport-only (no business logic). Added to Wave-3 block after `getShipmentTimeline`:

| Method | Endpoint | Used by |
|--------|----------|---------|
| `getWfirmaWebhookStatus()` | `GET /api/v1/webhooks/wfirma/status` | OperationalStatusStrip (Dashboard) |
| `getDeployStatus()` | `GET /api/v1/deploy/status` | OperationalStatusStrip (Dashboard) |
| `getProformaPipeline(batchId)` | `GET /api/v1/proforma/pipeline/{batch_id}` | PipelineKpiStrip (Proforma List) |
| `searchProformaDrafts(params)` | `GET /api/v1/proforma/search?...` | ProformaListPage "Search All Drafts" |
| `getDraftEvents(draftId)` | `GET /api/v1/proforma/draft/{id}/events` | HistoryTab (already existed, now also PL-5 context) |
| `suggestServiceCharges(draftId)` | `POST /api/v1/proforma/draft/{id}/suggest-service-charges` | ServiceChargesPanel |
| `applyServiceCharges(draftId, charges)` | `POST /api/v1/proforma/draft/{id}/apply-service-charges` | ServiceChargesPanel |
| `putServiceProduct(chargeType, body)` | `PUT /api/v1/proforma/service-products/{type}` | ServiceProductRegistryPanel |

---

## Browser verification

### Proforma List

- URL: `/v2/` SPA, Pro Forma nav → `?batch_id=SHIPMENT_LAS_TEST_2026-06`
- `data-testid="proforma-list-page"`: **CONFIRMED**
- `data-testid="proforma-pipeline-kpi-strip"`: **CONFIRMED** — shows Editing: 4 (amber), Total: 4, stage: "drafting"
- `data-testid="btn-new-proforma-draft"`: **CONFIRMED** (gold primary button)
- `data-testid="btn-import-packing-list"`: **CONFIRMED**
- `data-testid="kpi-editing"` value 4: **CONFIRMED** (after fixing `by_state` mapping bug)
- Table columns DRAFT ID, CLIENT, STATE, MATCH, CURRENCY, LINES, UPDATED: **CONFIRMED**
- Checkboxes per row: **CONFIRMED**
- Match chips showing "Unmapped": **CONFIRMED** (correct for unresolved customers)

### Proforma Detail

- Opened by clicking draft row (calls `onDrill(d)` → `handleProformaDrill` → `setProformaDraft`)
- Full 17-button toolbar: **CONFIRMED**
- Overview tab → ServiceProductRegistryPanel: **CONFIRMED** ("Service Charge → wFirma Product Registry", "+ Add mapping" button)
- Lines tab → 12-column table: **CONFIRMED** (# | PRODUCT | DESIGN NO | DESC (PL) | DESC (EN) | PURITY | HS CODE | ORIGIN | CUR | QTY | UNIT | NET) with horizontal scroll
- History tab → ACTIVITY HISTORY with "Draft created" event: **CONFIRMED**
- Console errors: **none** (Babel size warnings are pre-existing infrastructure)

---

## Bug fix during verification

**PipelineKpiStrip `by_state` mapping** — the initial implementation used `pipeline.counts` as the state key lookup. The actual API response uses `pipeline.by_state` (e.g. `{ editing: 4 }`). Fixed in same session before browser verification completed. All KPI tiles now show correct counts.

---

## Smoke tests

`cd service && pytest tests/ -m smoke -q` → **63 passed, 1 skipped**. No regressions.

---

## Files changed this session (Page B)

- `service/app/static/v2/proforma-list.jsx` — full rewrite: added `PipelineKpiStrip` (PL-1), enhanced table with checkboxes + Match chip (PL-2), `NewProformaDraftModal` (PL-3), `ImportPackingListModal` (PL-4); fixed `by_state` key during browser verification
- `service/app/static/v2/proforma-detail.jsx` — 4 edit points: (1) 12-column `ProformaLinesTab` with horizontal scroll (PD-2); (2) `_raw: ln` field in lines mapping; (3) `ServiceProductRegistryPanel` component (PL-5); (4) wired panel into Overview tab after `</ServiceChargesPanel>`
- `service/app/static/v2/pz-api.js` — 8 new Wave-3 transport methods added after `getShipmentTimeline`
