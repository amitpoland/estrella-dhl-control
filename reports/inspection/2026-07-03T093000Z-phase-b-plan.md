# Phase B gate-1 — Move Location disposition + Inventory parity plan

- **Date:** 2026-07-03 · read-only planning (STEP-0 ruling commit 4f6832b5
  was this run's only code-adjacent edit) · zero UI edits
- **Authorities:** design = operator wireframe (columns + actions preserved
  in reports/inspection/2026-07-02T-wfirma-wireframe-inspection.md);
  technical = `/v2/inventory` is the SOLE Inventory authority (operator
  Phase-B rules, DECISIONS "GOVERNANCE" 2026-07-03). Backend FROZEN
  (Phase A complete, 657a829c).

---

## a. MOVE LOCATION DISPOSITION — options for the operator (not decided here)

Blast-radius census (grep `move_location|move-location|MoveLocationPage`):
**7 files, 41 occurrences** — move-location-page.jsx (4), index.html (4:
script tag, render block, redirect-map preamble), components.jsx (2:
g_inventory child + comment), mock-badge.jsx (2: WIRED_PAGES + comment),
test_move_location_promotion.py (27 — the promotion pin suite),
test_sprint30_inventory_shell_wiring.py (1: slicer comment),
test_sprint43_coverage_authority_honest.py (1: count-pin comment).

### Option (i) — fold into the Inventory page now; retire the standalone page
- **What the wireframe shows:** Move Stock is an ACTION on the Inventory
  screen (toolbar: Upload Document / Move Stock / Cycle Count / Export) —
  there is NO standalone move page in the design authority. (i) matches it.
- **Shape:** the location-move function becomes a modal/panel inside
  InventoryPage (reusing PzApi.getInventoryState + movePieceLocation
  transports unchanged — pz-api.js untouched); move-location-page.jsx
  deleted; nav child removed (g_inventory group kept for future tabs or
  collapsed — sub-decision); `move_location` joins ROUTE_REDIRECTS →
  inventory (stale-URL insurance, 13 entries); WIRED_PAGES 19→18;
  sprint43 count pin 19→18.
- **Tests:** test_move_location_promotion.py (27 refs) REWRITTEN to pin the
  modal-in-Inventory (banner text, sequential semantics, error hints, and
  the window-collision pin all transfer); sprint30/43 comment/count touches.
- **Governance:** Lesson M relocation — requires a formal DECISIONS
  cancellation/relocation record + this pre-flight's approval, both
  explicitly (the GOVERNANCE roadmap note already flags it).
- **Effort:** M (one slice: modal + retirement + test rewrite + render
  check on cold origin). Pays the retirement cost ONCE.

### Option (ii) — keep the page short-term, parity-restyle, fold later
- **Shape:** move-location-page.jsx gains a KPI header + richer columns…
  but the merchandising columns need the JOINED READ that is
  **frozen-blocked** (see §c), so the restyle is mostly cosmetic now.
- **Tests:** small style pins now; the FULL (i) blast radius later anyway.
- **Wireframe:** does not depict a standalone move page — (ii) diverges
  from the design authority for its whole lifetime.
- **Effort:** S now + M later — pays the retirement cost TWICE.

---

## b. PARITY WORK BREAKDOWN for /v2/inventory (ordered; backend availability cited)

| # | Slice | Wireframe section | Component touched | Backend |
|---|---|---|---|---|
| B1 | KPI tile row polish | KPI tiles | `panel-stage2` StatBadges (inventory-page.jsx:281-286) | **AVAILABLE** — GET /inventory/stage2/aggregate; consignment tile stays honest backend-pending |
| B2 | Promotion Notes panel (the BE-2 v1 viewer) | document trail | new InvPanel on the EXISTING page (same idiom as panel-audit) | **AVAILABLE** — GET /inventory/promotion-notes/{batch}, /promotion-note/{note_no:path} (Phase A); shows the complete trail BE-2b just finished |
| B3 | proforma-detail:2542 real client_po | Client PO column | proforma-detail.jsx line mapper | **AVAILABLE** — client_po persisted since 494c4665; SELECT * readers already return it; fallback demoted to legacy-row default |
| B4 | Tab structure (fold family in) | tabbed subnav | SubTabStrip + InventoryPage internal panels; sample/return functions land as PANELS of the Inventory authority (never new pages — GOVERNANCE rule) | **AVAILABLE** — routes_inventory_sample + routes_inventory_returns are live (returns migration pending at deploy) |
| B5 | Action toolbar: Export + Upload Document | header actions | PageHeader actions on inventory route | **AVAILABLE** — Export = client-side CSV of live tables; Upload = link to Documents authority. (Move Stock action: see (i); Cycle Count: frozen, §c) |
| B6 | Move function per disposition (i) modal | Move Stock action | InventoryPage modal | **AVAILABLE** — existing pz-api transports |

Merchandising-grade table columns (Karat / Color / Quality / Dia Wt on the
piece tables) are NOT in this list — see §c.

## c. WHAT PHASE B CANNOT DO YET (frozen-blocked → Phase C intake)

1. **Merchandising columns joined read** — inventory_state ⋈ packing_lines
   (karat/stone_type/weights/qty exist in packing_lines; pieces[] carries
   only design_no). New read endpoint = backend; FROZEN.
2. **Manual Move Stock (business promotion) endpoint** — the manual
   exception path calling run_stock_promotion needs a new POST; FROZEN.
   The UI can show the action as `planned` (Lesson M five-state model),
   never fake it.
3. **Cycle Count** — no count model exists at all; net-new backend; FROZEN.
4. **Location-column in piece lists** (deferred since B×7-1) — same joined
   read as #1.
5. **Consignment tile going live** — consignment model net-new + the wFirma
   MM API answer still open (OQ-WFIRMA-MM-ANSWER); FROZEN twice over.
6. **Sample/return convert-to-sale, write-off, extend, credit/debit notes**
   (advertised in the design stubs) — no backend; FROZEN; UI shows
   `planned` states only.

Intake note: #1 + #4 are the same endpoint and the likely first Phase-C
unfreeze candidate — read-only, no fiscal surface, unblocks both tables.
