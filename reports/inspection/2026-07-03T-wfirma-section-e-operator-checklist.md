# Deliverable 1 §E — wFirma requirements checklist for Amit (durable copy)

Re-composed 2026-07-02 from the repo's live wFirma integration
(wfirma_client.py, routes_wfirma guards, docs/WFIRMA_API_RESEARCH.md) after
the original inspection report was lost in the chat transport channel;
originally delivered in-channel with the BE-1 commit report. Persisted to
disk per the standing rule: deliverables live on disk and in git; the
channel copy is a courtesy.

## API access
- [ ] API Key credentials: `accessKey`, `secretKey`, `appKey` (3-header
      method — Basic Auth deprecated by wFirma 2023-07-02; all modules need
      the keys)
- [ ] `company_id` for the Estrella company in wFirma (multi-company
      accounts must pin it)
- [ ] Confirm the API user is ACTIVE and keys are stored only in service
      env vars — never in the repo

## Permissions
- [ ] API user rights for warehouse modules: `warehouse_document_r/p_z/w_z/
      z_d` (read + add) and `goods`, `contractors`, `invoices`
- [ ] KSeF is a SEPARATE grant: not inherited from the administrator — the
      API user needs its own KSeF certificate/token and the KSeF module
      activated
- [ ] Note: invoices created via API go to KSeF **immediately** — there is
      no draft stage via API

## Warehouse IDs
- [ ] `WFIRMA_WAREHOUSE_ID` — the target warehouse for PZ (already required
      by the create guard)
- [ ] List ALL warehouses that exist in wFirma today: is stock one
      warehouse, or Temp/Final as two? (decides whether promotion ever
      needs a wFirma transfer document or stays app-side only)
- [ ] **MM (przesunięcie międzymagazynowe) via API — ask wFirma support
      directly.** MM is absent from the client registry, python-wfirma's
      documented types, and all four repo docs (scope 6d6d9d64 §B). The
      consignment flow is PARKED on this answer (BE-1c / consignment).
- [ ] `WFIRMA_SUPPLIER_CONTRACTOR_ID` confirmed for each active supplier
      (supplier master `wfirma_id` filled)

## Numbering series
- [ ] PZ number series in wFirma (the app reads the fullnumber back after
      create — confirm the series format won't change mid-year)
- [ ] WZ number series — needed for the future sale-side out document;
      confirm it exists / who owns it. Also verify: WZ add via API vs the
      forum claim that WZ is only auto-generated by issuing an invoice —
      one sandbox probe decides the sale-out-leg implementation shape
- [ ] Stock Promotion Note series (SPN/NNN/YYYY) is **local to Atlas** —
      shipped in BE-2 (a87dd7d8); nothing to set up in wFirma

## Sandbox / safety
- [ ] Is there a test company or sandbox in the wFirma account for trial
      `warehouse_document` writes? If not: first live writes happen only
      behind `WFIRMA_CREATE_PZ_ALLOWED` with operator watching
- [ ] Confirm all fiscal gates stay OFF until each flow is verified:
      `WFIRMA_CREATE_PZ_ALLOWED`, `WFIRMA_CREATE_PRODUCT_ALLOWED`,
      proforma/invoice gates
- [ ] Stock read (goods count/reserved): `get_stock` is a
      NotImplementedError stub (wfirma_client.py:1161) — enabling it is
      the verification read for the consignment double-stock-out guard
